SESSION_ID: 2026-03-21_02b
CHANGES:
- Removed `import("gameplay/ags_special_population_ui.scar")` from cba.scar
- Removed direct `AGS_SpecialPopulationUI_OnPlay()` call from `Mod_Start()` in cba.scar
- Added `import("gameplay/ags_limits_ui.scar")` to cba.scar (prior session)
- Added `import("gameplay/cba_economy.scar")` to cba.scar (prior session)
- Fixed `EGroup_CountMember` → `EGroup_CountSpawned` + 0-based → 1-based iteration in cba_debug_stress.scar tests 7.4/7.4b (prior session)
- Updated stress test 7.1 function list to match actual implemented functions (prior session)

FIXES:
- `AGS_LimitsUI_Update` nil error: ags_limits_ui.scar was never imported
- Old siege UI panel rendering alongside new limits UI: direct OnPlay bypass in Mod_Start
- `EGroup_CountMember` does not exist in SCAR API; correct function is `EGroup_CountSpawned`
- EGroup iteration is 1-based (`for i = 1, count`), not 0-based

NEW PATTERNS:
- RULE: EGroup iteration pattern is `for i = 1, EGroup_CountSpawned(eg) do ... EGroup_GetEntityAt(eg, i)`
- RULE: `EGroup_CountMember` does not exist; use `EGroup_CountSpawned` or `EGroup_CountAlive`
- RULE: Module `Core_RegisterModule` does not prevent direct function calls from bypassing `UpdateModuleSettings` disable; always remove explicit call sites when retiring a module
- RULE: Separate UI panel IDs (`UI_AddChild` name param) render independently; removing an import is sufficient to stop rendering

FAILURES:
- Validation 3.5c: `CBA_CIV_BP_SUFFIX` is nil — stale test referencing removed table
- Stress 7.4: Outpost startup reconciliation missing `stone_wall_tower` type check — stored=0 actual=2 for all players
- Coverage 5.1: 6/8 — expects `CBA_Options_RamLimit_Disable`, `CBA_Options_SiegeTowerLimit_Disable` (unimplemented)

NEW/UPDATED FILES:
- EDITED: Gamemodes/Onslaught/assets/scar/cba.scar
- EDITED: Gamemodes/Onslaught/assets/scar/debug/cba_debug_stress.scar

GAPS:
- cba_options.scar L196: startup reconciliation counts `scar_outpost` only, not `stone_wall_tower` — causes outpost counter desync on maps with pre-placed wall towers
- cba_debug_validation.scar test 3.5c: references deleted `CBA_CIV_BP_SUFFIX` table
- cba_debug_coverage.scar test 5.1: expects 2 unimplemented ram/siege-tower functions
- Validation 2.6 team display prints table address instead of team index (cosmetic)

CRITICAL DECISIONS:
- ags_special_population_ui.scar file kept on disk (not deleted) to minimize risk; only load path removed
- Ram/siege tower limit functions confirmed planned-not-implemented; test expectations updated to match reality

NEXT ACTIONS:
- Fix cba_options.scar L196: add `or Entity_IsOfType(eid, "stone_wall_tower")` to startup outpost reconciliation
- Remove stale test 3.5c from cba_debug_validation.scar
- Remove 2 unimplemented function names from cba_debug_coverage.scar test 5.1
- Optional: fix validation 2.6 team string to use `player.team.index`
