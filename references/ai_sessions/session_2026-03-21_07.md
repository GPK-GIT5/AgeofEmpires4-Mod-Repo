SESSION_ID: 2026-03-21_07
CHANGES:
- Researched scenario leaver handling patterns for reuse in Onslaught pipeline
- Documented `BLUEPRINT_REVERSE_MAP` dynamic build from AGS_ENTITY_TABLE (attribName → AGS key)
- Documented `_ConvertBuildingEntity()` delete+respawn with health% preservation pattern
- Identified AGS_ENTITY_TABLE has only 7 unit keys — no military combat units

FIXES:
- None (research-only session)

NEW PATTERNS:
- RULE: AGS_ENTITY_TABLE contains 0 military combat unit BPs — military conversion requires suffix-swap via AGS_CIV_PREFIXES, not table lookup
- RULE: Snapshot-then-iterate for entity/squad collections — avoids mutation during iteration when destroying+recreating
- RULE: Scenario recipient selection = lowest pop + fewest prior transfers as tie-break
- RULE: `_BuildReverseLookups()` must iterate all civs × all keys to build complete reverse map
- RULE: Scenarios convert villagers (delete+respawn), Onslaught kills eco — design divergence is intentional

FAILURES:
- None

NEW/UPDATED FILES:
- `/memories/session/leaver-conversion-design.md` (session memory, research findings)
- `/memories/session/plan.md` (session memory, 14-step implementation plan)

GAPS:
- Multi-phase conversion system design in-progress (todo incomplete)
- Limit enforcement + overflow redistribution design not started
- No code changes made — research only

CRITICAL DECISIONS:
- Military unit conversion via AGS_CIV_PREFIXES suffix-swap (not AGS_ENTITY_TABLE)
- Building conversion via delete+respawn (not ownership transfer) for cross-civ blueprint correctness
- Eco squads killed (Onslaught policy) vs converted (scenario policy)
- Overflow redistribution to allies when primary recipient at capacity

NEXT ACTIONS:
1. Design multi-phase conversion system (8 phases) incorporating scenario patterns
2. Design limit enforcement + overflow redistribution logic
3. Implement `_Leaver_WarmupPrecache()` for engine red-box prevention
4. Implement wall tower transfer pattern fix (`defense_tower`)
5. Implement age-gate validation table
