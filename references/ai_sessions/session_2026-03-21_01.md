SESSION_ID: 2026-03-21_01
CHANGES:
- `cba_options.scar`: Added else-unlock branch to `RamLimit_ApplyThreshold` (~L1286)
- `cba.scar`: Removed ram BPs from siege cascade tier 2 in `SiegeLimit_Disable` (~L610)
- `cba.scar`: Fixed `SquadKilled` handler — `Util_GetPlayerOwner(context.victim)` → `Squad_GetPlayerOwner(squad)`
- `cba_siege_data.scar`: Fixed 4 CBA_SIEGE_TABLE entries (abbasid_ha_01/chinese_ha_01/french_ha_01 siege_tower + chinese_ha_01 ram) — base civ BPs → HA variant BPs
- `rewards/cba_rewards.scar`: `SiegeLimit_ApplyThreshold` → `RevalidateAll` on Siege Engineers grant

FIXES:
- **Ram permanent lock**: `RamLimit_ApplyThreshold` was lock-only; cascade tier 2 managed ram state creating fragile dependency. Fix: bidirectional threshold + decouple rams from cascade.
- **Knights DLC siege tower bypass**: 3 HA civs had base-civ siege_tower strings → `SiegeTowerLimit_Disable` locked wrong production item.
- **SquadKilled fragile API**: `Util_GetPlayerOwner` on squad context replaced with `Squad_GetPlayerOwner`.
- **Inactive rewards module**: Missing `FieldConstruct_Gate` call after Siege Engineers grant.

NEW PATTERNS:
- RULE: Every `ApplyThreshold` function MUST have bidirectional lock/unlock branches.
- RULE: `FieldConstruct_Gate` MUST run AFTER `SiegeLimit_ApplyThreshold` in every event path — it overrides cascade unlocks for civs without `AGS_UP_SIEGE_ENGINEERS`.
- RULE: All CBA_SIEGE_TABLE entries for Knights DLC civs (abbasid_ha_01, chinese_ha_01, french_ha_01, hre_ha_01) require `_ha_01` suffix on unit BPs. Dynasties DLC civs use parent map fallback.
- RULE: Use `CBA_Options_RevalidateAll(player)` (not individual threshold calls) when granting upgrades that affect multiple limit systems.

FAILURES:
- None. All planned fixes applied successfully.

NEW/UPDATED FILES:
- `Gamemodes/Onslaught/assets/scar/cba.scar` (modified)
- `Gamemodes/Onslaught/assets/scar/gameplay/cba_options.scar` (modified)
- `Gamemodes/Onslaught/assets/scar/gameplay/cba_siege_data.scar` (modified)
- `Gamemodes/Onslaught/assets/scar/rewards/cba_rewards.scar` (modified)

GAPS:
- No explicit Age gate for Abbasid/Mongol field construction — gated only by 100-kill reward. If player reaches 100 kills in Dark Age, field-construct unlocks.
- `rewards/cba_rewards.scar` is not imported by default (`cba.scar` imports `rewards.scar`). Fix is preventive only.
- `MaximumMedicPopulation == 0` disables all siege gating including `FieldConstruct_Gate` — appears intentional but unverified.

CRITICAL DECISIONS:
- Decoupled rams from siege cascade tier 2 entirely rather than adding conditional logic.
- Chose `RevalidateAll` over `FieldConstruct_Gate` alone for reward grant path — ensures all limits stay consistent.

NEXT ACTIONS:
- Verify in-game that ram unlock/relock works after kills at threshold boundary.
- Verify abbasid_ha_01/chinese_ha_01/french_ha_01 siege towers now correctly gated.
- Decide whether Abbasid/Mongol field-construct needs an Age floor in `FieldConstruct_Gate`.
