SESSION_ID: 2026-03-21_01
CHANGES:
- `cba.scar`: Added `_Leaver_WarmupPrecache()` — precaches 17 building keys × all players at `Mod_Start`
- `cba.scar`: Added `_LEAVER_AGE_REQUIREMENTS` table — gates university/arsenal/castle (Age 3), wonder (Age 4)
- `cba.scar`: Wall transfer pattern expanded: `defense_tower` added alongside `defense_wall`/`palisade`
- `cba.scar`: Building creation failure fallback: `Entity_Kill` → `_Transfer_BuildingToPlayer` (keep foreign BP)
- `cba.scar`: Squad creation failure fallback: `Squad_Kill` → `Squad_SetPlayerOwner` (keep foreign BP)
- `cba_debug_leaver.scar`: Visual test expanded from 10 → ~28 entities (17 buildings in 5 categories + 11 units)
- `cba_debug_leaver.scar`: Stage 5 audit skip list now includes `palisade` patterns

FIXES:
- Defense towers (`building_defense_tower_*`) no longer killed on conversion — transferred instead
- Engine red-box overlay prevented via fire-and-forget `Entity_Precache` warmup at match start
- Creation failures no longer destroy assets — foreign-BP transfer used as safe fallback
- Age-inappropriate buildings (e.g., university for Age 1 receiver) now killed instead of spawned

NEW PATTERNS:
- RULE: `Entity_Precache(ebp, 0, player.id, "", source, id)` is async fire-and-forget; no callback needed
- RULE: `Entity_CreateFacing` succeeds even without precache — red box is visual-only, not a Lua error
- RULE: No `Misc_IsEBPPrecached` exists in SCAR API — cannot check precache status
- RULE: Preferred failure policy = transfer ownership (keep foreign BP) over kill
- RULE: Age-gate checks go AFTER same-BP optimization, BEFORE `Entity_CreateFacing`

FAILURES:
- None in this session. All edits are code-only; no runtime verification performed.

NEW/UPDATED FILES:
- `Gamemodes/Onslaught/assets/scar/cba.scar` (modified)
- `Gamemodes/Onslaught/assets/scar/debug/cba_debug_leaver.scar` (modified)

GAPS:
- No runtime verification — Visual/Full/Stress tests not yet executed in-game
- `Entity_Precache` parameter correctness unverified (skinItemDefinitionID=0, empty resourceContainerCacheName)
- Squad precache (`Squad_Precache`) not implemented — only building EBPs precached
- Age-gate table covers only 4 keys — other age-locked buildings (e.g., civ-specific) not gated

CRITICAL DECISIONS:
- Precache IS needed: `Entity_CreateFacing` succeeds without it, but engine renders red-box overlay
- Transfer-on-failure chosen over kill-on-failure per user directive
- Precache runs in `Mod_Start` (post-fade-in) — earliest safe hook with PLAYERS table populated

NEXT ACTIONS:
1. `Debug_LeaverConversion_Visual(2,1)` — verify 17 buildings + 11 units across 5 categories
2. `Debug_LeaverConversion_Full()` — confirm 0 creation failures
3. `Debug_LeaverConversion_Stress(3)` — multi-disconnect: towers/walls transfer, no red boxes
4. Check scarlog for `_Leaver_WarmupPrecache` count message
5. Verify no red-box overlay after precache warmup
6. Consider adding `Squad_Precache` for military units if red-box persists on squad conversion
