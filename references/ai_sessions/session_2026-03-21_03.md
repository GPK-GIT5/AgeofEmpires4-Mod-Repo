SESSION_ID: 2026-03-21_03

CHANGES:
- Rewrote `cba_debug_advanced_stress.scar`: all anonymous fns → named globals, master runner → 6-phase `_AdvStressAll_Phase2..6` + `_FinalReport` chain
- Rewrote `Debug_AdvStress_RapidElimination`: nested `_visual.callback` chain → 4 named stage fns (`_RapidElim_DestroyP2/P3/P4/FinalReport`)
- Rewrote `Debug_VisualValidation_KeepDestroy`: anonymous `Rule_AddOneShot` → named `_VisualKeepDestroy_Execute` + `_Finish`
- Removed slow-motion from visual sequences; replaced with `Camera_MoveTo` + `UI_CreateEventCue`
- Added `Debug_RunAll(step)` master orchestrator to `cba_debug_core.scar` — chains 6 suites via EndMarker hook
- Added master advancement hook to `_Debug_EndMarker` — detects `_cba_debug._master.awaiting` and auto-chains
- `Debug_RunSmokeTests()` now returns `pass, total`
- Fixed `Debug_EventProbes()` nil crash: `_cba_debug.results` → local `results` table passed to all `_EP_Group_*` fns
- Standardized all suite totals to `TOTAL: X/Y passed` format before EndMarker

FIXES:
- FATAL: `Rule_AddOneShot(function()...end)` → "Adding unnamed function as rule" (3 sites)
- FATAL: `#_cba_debug.results` → nil (field never existed; probes never recorded results)

NEW PATTERNS:
- RULE: `Rule_AddOneShot`/`Rule_AddInterval` require named global functions. Anonymous fns fatal.
- RULE: `_Debug_ScheduleNextPhase` inherits same constraint (wraps `Rule_AddOneShot`).
- RULE: Visual debug uses camera+cue staging, not slow-motion. `Debug_SlowMotion` available but opt-in only.
- RULE: Master suite chaining: EndMarker fires → checks `_master.awaiting` match → calls `_Debug_MasterAdvance`.
- RULE: Every suite prints `TOTAL: X/Y passed` immediately before `_Debug_EndMarker`.

FAILURES:
- Stress test 7.4 outpost count: stored=0 actual=2 for all players (pre-existing, not addressed)

NEW/UPDATED FILES:
- MODIFIED: `debug/cba_debug_advanced_stress.scar`
- MODIFIED: `debug/cba_debug_helpers.scar`
- MODIFIED: `debug/cba_debug_core.scar`
- MODIFIED: `debug/cba_debug_event_probes.scar`
- MODIFIED: `debug/cba_debug_scenario.scar`
- UPDATED: `.skills/scar-debug/SKILL.md`

GAPS:
- Outpost counter initialization (7.4 fail) unresolved
- Visual debug expansion (V1-V6 scenarios) planned but not implemented
- `_Debug_VisualAssert` helper not yet built
- `Debug_VisualAll()` runner not yet created

CRITICAL DECISIONS:
- Step mode no longer forced inside scenarios — user enables before calling
- Hook registrations use named functions, not anonymous wrappers
- Master suite tracks results via EndMarker string matching, not callback wiring

NEXT ACTIONS:
- Implement `_Debug_VisualAssert(desc, check_fn, pos)` in helpers.scar
- Build V4 (Siege Threshold Cascade) as first visual scenario
- Investigate outpost counter init (stress 7.4 regression)
- In-game retest: `Debug_RunAll()` end-to-end
