SESSION_ID: 2026-03-21_02
CHANGES:
- Unified siege limit system: 12 edits across cba_options.scar, cba.scar, rewards.scar
- Added `CBA_Options_FieldConstruct_Gate`: locks ALL field-construction EBPs until Siege Engineers upgrade obtained
- Wired FieldConstruct_Gate into 7 call sites: PresetFinalize init loop, BuildItemComplete, SquadKilled, ConstructionStart, ConstructionCancelled, EnforcePostTransfer, RevalidateAll
- Removed overflow early-return in `_Convert_Building` so all buildings hit AGS conversion path
- Clamped `Entity_GetHealthPercentage()` to [0,1] with nil fallback in `_Convert_Building`

FIXES:
- Bug A: `RamLimit_ApplyThreshold` else/UNLOCKED branch removed — lock-only now, cascade handles unlock
- Bug B: Duplicate inline siege blocks removed from `BuildingLimit_ConstructionStart` and `BuildingLimit_ConstructionCancelled`
- Bug C: `EnforcePostTransfer` now includes ram/ST recount + threshold + FieldConstruct_Gate
- Bug D: `_Convert_Building` overflow else branch did `_Transfer_BuildingToPlayer + return`, bypassing AGS conversion — caused foreign BP transfers (sultanate mosque → japanese)
- Bug E: `Entity_GetHealthPercentage()` returns >1.0 with HP modifiers — clamped to [0,1]

NEW PATTERNS:
- RULE: Never early-return from `_Convert_Building` before AGS reverse-lookup — all buildings must attempt conversion
- RULE: Always clamp engine health/percentage APIs before arithmetic — HP modifiers can push outside [0,1]
- RULE: Field-construction locks use `Player_SetEntityProductionAvailability(ITEM_REMOVED)`, workshop locks use `Player_SetSquadProductionAvailability(ITEM_REMOVED)`
- RULE: `FieldConstruct_Gate` must run LAST in every threshold evaluation path (after cascade unlock)

FAILURES:
- None — both scarlogs validated clean after limit system edits

NEW/UPDATED FILES:
- `gamemodes/Onslaught/assets/scar/gameplay/cba_options.scar` — 9 edits (FieldConstruct_Gate, Bug A/B/C, EnforcePostTransfer, RevalidateAll)
- `gamemodes/Onslaught/assets/scar/cba.scar` — 4 edits (FieldConstruct_Gate wiring, health clamp, overflow bypass fix)
- `gamemodes/Onslaught/assets/scar/rewards.scar` — 1 edit (RevalidateAll replaces manual ITEM_DEFAULT overrides)

GAPS:
- Capital TC foreign BP (`building_town_center_capital_jpn` transferred as-is) — pre-existing, not addressed
- Reward-spawned siege squads bypass `siegeLimit_count` increment — known drift vector, not yet fixed

CRITICAL DECISIONS:
- Mongol `castle = false` in AGS_ENTITY_TABLE is intentional (no keep) — runtime fallback log is expected, not a bug
- FieldConstruct_Gate covers: generic ram/ST, Mongol buildable, Abbasid buildable, Abbasid HA buildable+ram_tower, Byzantine fire ram
- Overflow branch now falls through to conversion instead of early-return — Phase 7 enforcement handles over-limit

NEXT ACTIONS:
- Playtest sultanate→japanese leaver to confirm mosque converts to `building_unit_religious_buddhist_jpn`
- Playtest HP-modified building transfer to confirm no `Invalid healthPercent` error
- Investigate capital TC conversion (separate task)
- Consider incrementing `siegeLimit_count` in reward spawn paths (`UnitEntry_DeploySquads`)
