SESSION_ID: 2026-03-21_03
CHANGES:
- Added `_Leaver_WarmupPrecache()` at L816: fire-and-forget `Entity_Precache` for 17 building keys × all players at `Mod_Start` (L264)
- Added `_LEAVER_AGE_REQUIREMENTS` table at L841: university/arsenal/castle=3, wonder=4
- Age-gate check at L1088 kills building if receiver age < required; runs after same-BP optimization, before `Entity_CreateFacing`
- Wall transfer pattern at L1071 now includes `defense_tower` alongside `defense_wall`/`palisade`
- Building creation failure fallback at L1113: `_Transfer_BuildingToPlayer` replaces `Entity_Kill`
- Squad creation failure fallback at L1328: `Squad_SetPlayerOwner` replaces `Squad_Kill`
- Visual test expanded: 5 building groups (mil×4, econ×4, adv×4, walls×3, landmarks×2 = 17) + 11 unit types (infantry/ranged/cavalry/siege)
- Stage 5 audit skip list expanded: added `palisade` pattern to foreign-BP exclusion

FIXES:
- Defense towers (`building_defense_tower_*`) no longer killed on conversion — transferred
- Engine red-box overlay prevented via async `Entity_Precache` warmup
- Creation failures preserve assets via transfer-ownership fallback
- Age-inappropriate buildings killed instead of spawned at wrong age

NEW PATTERNS:
- RULE: `Entity_Precache(ebp, 0, player.id, "", src_string, id_string)` — async, no callback, prevents red-box
- RULE: `Entity_CreateFacing` succeeds without precache — red-box is visual-only, not Lua error
- RULE: No `Misc_IsEBPPrecached` in SCAR API — cannot query precache status
- RULE: Failure policy = transfer ownership (foreign BP) over kill
- RULE: Age-gate placement: after same-BP check, before `Entity_CreateFacing`

FAILURES:
- None (code-only session, no runtime verification)

NEW/UPDATED FILES:
- `Gamemodes/Onslaught/assets/scar/cba.scar` — precache, age-gate, wall fix, fallbacks
- `Gamemodes/Onslaught/assets/scar/debug/cba_debug_leaver.scar` — expanded visual + audit

GAPS:
- No runtime verification yet
- `Squad_Precache` not implemented — only EBPs precached
- `Entity_Precache` param correctness unverified (skinItemDefinitionID=0, empty cache name)
- Age-gate covers 4 keys only — civ-specific age-locked buildings ungated

CRITICAL DECISIONS:
- Precache needed: creation succeeds without it, but engine renders red-box overlay
- Transfer-on-failure over kill-on-failure (user directive)
- Precache in `Mod_Start` — earliest hook with PLAYERS populated

NEXT ACTIONS:
1. `Debug_LeaverConversion_Visual(2,1)` — verify 17 buildings + 11 units
2. `Debug_LeaverConversion_Full()` — 0 creation failures expected
3. `Debug_LeaverConversion_Stress(3)` — towers transfer, no red boxes
4. Check scarlog for `_Leaver_WarmupPrecache` count
5. Verify no red-box overlay post-warmup
6. Consider `Squad_Precache` if red-box persists on squad conversion
