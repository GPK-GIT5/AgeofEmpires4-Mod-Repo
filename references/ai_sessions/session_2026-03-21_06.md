SESSION_ID: 2026-03-21_06
CHANGES:
- Created `cba_debug_ui_prompts.scar` — modular UI-driven question system using XamlPresenter overlay
- Two prompt APIs: `Debug_AskYesNo()` (2 buttons) and `Debug_AskChoice()` (2-4 buttons)
- Console fallback: `Debug_Answer("yes"|"no"|1..4|"a".."d")`, `Debug_DismissPrompt()`
- Three pre-built interactive checks: Precache blueprint scan, Keep state visual confirm, Siege limit UI confirm
- Suite runner `Debug_InteractiveChecks()` chains all checks with idle-polling between them
- Wired into `Debug_RunAll()` as 7th suite (was 6)

FIXES:
- None (greenfield implementation)

NEW PATTERNS:
- RULE: XamlPresenter interactive panels require `IsHitTestVisible = true` for button clicks
- RULE: `UI_CreateCommand("GlobalFnName")` binds XAML Button.Command to a named global SCAR function
- RULE: `UI_SetDataContext(panel_name, dc_table)` updates bindings after initial creation
- RULE: Use idle-polling (`Rule_AddInterval` checking `active == false`) to chain async prompt sequences
- RULE: All `Rule_AddOneShot`/`Rule_AddInterval` callbacks must be named global functions (no anonymous)
- RULE: Auto-timeout with default choice ensures prompts never block autonomous runs
- RULE: Safe XAML bindings: Text, Foreground, Visibility+BoolToVis, Command. Fatal: Width, Height, Opacity

FAILURES:
- None

NEW/UPDATED FILES:
- NEW: `Gamemodes/Onslaught/assets/scar/debug/cba_debug_ui_prompts.scar`
- EDIT: `Gamemodes/Onslaught/assets/scar/debug/cba_debug.scar` (added import)
- EDIT: `Gamemodes/Onslaught/assets/scar/debug/cba_debug_core.scar` (module registry + RunAll queue)

GAPS:
- No runtime test — requires in-game AoE4 session to verify XamlPresenter renders
- KeepState check iterates sequentially; no batch/parallel keep inspection
- Siege limit check uses hardcoded mangonel spawn name pattern — may fail for some civs
- No `_DebugPrompt_Cleanup()` call wired into game-end teardown yet

CRITICAL DECISIONS:
- Panel created once on first use, reused via DataContext updates (not recreated per prompt)
- InteractiveChecks placed LAST in Debug_RunAll queue (after all autonomous suites)
- Timeout defaults: 30s general, 15s for auto-pass precache, 20s for keep/siege checks
- Skip counts as non-blocking pass in precache 3-choice prompt

NEXT ACTIONS:
- Wire `_DebugPrompt_Cleanup()` into game-end / debug teardown path
- Runtime test all 3 interactive checks in-game
- Add more interactive checks: ownership transfer confirm, sacred site capture confirm
- Expand siege limit check to resolve SBP per-civ from AGS_ENTITY_TABLE instead of hardcoded name
- Consider adding `Debug_InteractiveChecks()` as standalone console entry in help/docs
